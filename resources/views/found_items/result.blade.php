<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Result Laporan #{{ $item->id }}</title>
    <style>
        body { font-family: 'Arial', sans-serif; color: #333; padding: 30px; line-height: 1.5; }
        .header { text-align: center; border-bottom: 2px solid #4fd1c5; padding-bottom: 15px; margin-bottom: 25px; }
        .box { border: 1px solid #ddd; padding: 20px; border-radius: 10px; }
        .label { font-weight: bold; color: #666; font-size: 11px; text-transform: uppercase; margin-top: 15px; }
        .value { font-size: 15px; border-bottom: 1px solid #f0f0f0; padding-bottom: 5px; }
        .photo-container { text-align: center; margin-top: 20px; }
        .img-result { max-width: 250px; border-radius: 8px; border: 1px solid #eee; }
        .footer { margin-top: 40px; text-align: center; font-size: 10px; color: #aaa; }
        @media print { .no-print { display: none; } }
    </style>
</head>
<body onload="window.print()">
    <div class="header">
        <h2 style="margin:0;">RESULT LAPORAN TEMUAN</h2>
        <small>Campus Lost & Found System</small>
    </div>

    <div class="box">
        <div class="label">Nomor ID</div>
        <div class="value">#{{ $item->id }}</div>

        <div class="label">Nama Barang</div>
        <div class="value">{{ $item->nama_barang }}</div>

        <div class="label">Lokasi Ditemukan</div>
        <div class="value">{{ $item->lokasi_ditemukan }}</div>

        <div class="label">Tanggal Ditemukan</div>
        <div class="value">{{ \Carbon\Carbon::parse($item->tanggal_ditemukan)->format('d/m/Y') }}</div>

        <div class="label">Status</div>
        <div class="value">{{ $item->status }}</div>

        <div class="label">Deskripsi</div>
        <div class="value">{{ $item->deskripsi }}</div>
        
        <div class="photo-container">
            <div class="label">Lampiran Foto</div>
            @if($item->foto_barang)
                <img src="{{ asset('storage/' . $item->foto_barang) }}" class="img-result">
            @else
                <p class="value">Tidak ada foto</p>
            @endif
        </div>
    </div>

    <div class="footer">
        Generated by System: {{ now()->format('d/m/Y H:i') }}
    </div>

    <div class="no-print" style="margin-top: 20px; text-align: center;">
        <button onclick="window.print()" style="padding: 8px 15px; cursor:pointer;">Cetak Result</button>
        <a href="{{ route('found-items.index') }}" style="margin-left: 10px; font-size: 14px; color: teal;">Kembali ke Dashboard</a>
    </div>
</body>
</html>